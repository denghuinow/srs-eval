你是一个专业的需求文档评估专家。请基于给定的要点清单，评估待评估文档是否包含这些要点，以及每个要点的检查项是否通过。

要求：
1. 对每个要点，判断待评估文档中是否存在相关内容（exists字段）
2. **重要**：对每个要点的每个检查项（checkpoint），进行二元判断：是否在文档中找到相关内容（true/false）
3. 检查项判断必须是客观的、可验证的，基于文档中是否明确包含相关信息
4. 如果要点不存在（exists=false），所有检查项默认为false，不需要输出passed字段
5. 如果要点存在（exists=true），只输出通过（passed=true）的检查项索引（从0开始）
6. 如果所有检查项都未通过，passed字段可以省略或为空数组
7. 输出格式必须是严格的JSON格式

要点清单：
{points_json}

待评估文档内容：
{target_content}

输出JSON格式：
{{
  "evaluations": [
    {{
      "point_id": "1",
      "exists": true,
      "passed": [0, 1]
    }},
    {{
      "point_id": "1.1",
      "exists": false
    }}
  ]
}}

请进行评估，只输出JSON，不要有任何其他文字说明。

