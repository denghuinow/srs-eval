你是一个专业的需求文档评估专家。请基于给定的检查项清单，评估待评估文档是否包含这些检查项。

要求：
1. 对每个检查项（checkpoint），进行二元判断：是否在文档中找到相关内容（true/false）
2. 检查项判断必须是客观的、可验证的，基于文档中是否明确包含相关信息
3. 只输出通过（passed=true）的检查项索引数组
4. checkpoint_index从0开始
5. 未输出的检查项默认为未通过
6. 如果所有检查项都未通过，可以输出空数组[]
7. 输出格式必须是严格的JSON格式

检查项清单：
{checkpoints_json}

待评估文档内容：
{target_content}

输出JSON格式（数组格式）：
[0, 1, 5, 10]

或者字典格式：
{{
  "passed_indices": [0, 1, 5, 10]
}}

请进行评估，只输出JSON，不要有任何其他文字说明。

